<?php

use function Tests\assertInertiaComponent;

it('has app/actions/usepositapp/useproposalnewchooseorg page', function () {
    // TODO
    // $response = $this->get(route('use.proposal.new.choose-org'));

    // $response->assertStatus(200);
    // assertInertiaComponent($response, 'Use/ProposalNewChooseOrg');
});
